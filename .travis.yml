language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-aryaangular' ]; then cd .. && eval "mv $currentfolder generator-aryaangular" && cd generator-aryaangular; fi

